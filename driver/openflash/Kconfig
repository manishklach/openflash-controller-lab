config OPENFLASH
	tristate "OpenFlash experimental NAND controller"
	depends on PCI && BLOCK
	help
	  Experimental driver scaffold for the OpenFlash managed NAND controller.
	  This option does not expose a block device until the queue ABI is implemented.

